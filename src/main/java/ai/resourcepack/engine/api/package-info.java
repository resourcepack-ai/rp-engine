/**
 * The supported surface of RP Engine.
 *
 * <p><strong>Everything in this package is API. Everything under
 * {@code ai.resourcepack.engine.core} is not</strong>, and may change in any
 * release including a patch. It is public only because javac demanded it
 * across a package boundary; public has never meant supported here.
 *
 * <p>If you find yourself wanting to reach into {@code core}, that is the
 * signal that something belongs in this package — not that the reach is fine.
 */
package ai.resourcepack.engine.api;
